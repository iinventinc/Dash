import 'package:flutter/material.dart';

class getHelpSupport extends StatelessWidget {
  const getHelpSupport({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(children: [


        
      ],)
      
    );
  }
}