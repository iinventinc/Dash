import 'package:dash/Screens/exportscreen.dart';
import 'package:flutter/material.dart';

class BecomeaDasher extends StatelessWidget {
  const BecomeaDasher({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(

      appBar: MyAppBar(mytitle: "Become a Driver",),
      
    );
  }
}