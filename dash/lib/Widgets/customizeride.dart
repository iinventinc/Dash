import 'package:dash/Widgets/exportscreen.dart';
import 'package:flutter/material.dart';

class CustomizeRide extends StatelessWidget {
  const CustomizeRide({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(

 appBar: MyAppBar(mytitle: "Customize Ride",),


    );
  }
}